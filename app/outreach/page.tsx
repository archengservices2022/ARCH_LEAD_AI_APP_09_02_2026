import Shell from "../components/Shell";import OutreachControls from "./OutreachControls";import{getDb,getSettings}from"../../lib/db";export const dynamic="force-dynamic";export default async function Outreach(){const db=getDb(),s=await getSettings();const msgs=await db.prepare(`SELECT o.id,l.company,o.division,o.recipient,o.sender,o.subject,o.body,o.status,o.attempt_number,o.sent_at,o.error_message,o.created_at FROM outreach_messages o LEFT JOIN leads l ON l.id=o.lead_id WHERE o.status NOT IN ('Invalid Contact','Rejected') ORDER BY o.created_at DESC LIMIT 200`).all<any>();return <Shell active="/outreach" dryRun={s.dryRun}><header><div><h1>Outreach</h1><p>Verified lead promotion, personalized drafts and controlled sending</p></div></header><div className="safeBanner"><b>Dry Run:</b> prepare and review messages here. No email can be sent while Dry Run is ON.</div><article className="panel wide"><div className="panelHead"><div><h2>Outreach Preparation</h2><p>Only qualified leads with a strictly verified public contact identity and non-suppressed email can enter the draft queue.</p></div></div><OutreachControls/></article><article className="panel wide"><div className="panelHead"><div><h2>Approval Queue / Message Ledger</h2><p>Only valid outreach drafts are shown here. Invalid contact matches are automatically excluded.</p></div></div>{msgs.results.length?<div className="tableWrap"><table><thead><tr><th>Company</th><th>Division</th><th>Recipient</th><th>Subject / Draft</th><th>Status</th><th>Sent</th></tr></thead><tbody>{msgs.results.map((m:any)=><tr key={m.id}><td><b>{m.company||"—"}</b></td><td>{m.division}</td><td>{m.recipient}</td><td><details><summary>{m.subject}</summary><pre style={{whiteSpace:"pre-wrap",maxWidth:620}}>{m.body}</pre></details></td><td>{m.status}</td><td>{m.sent_at||"Not sent"}</td></tr>)}</tbody></table></div>:<div className="empty">No valid drafts currently. Find and verify a real decision-maker before preparing outreach.</div>}</article></Shell>}
