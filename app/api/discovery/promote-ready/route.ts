import{getDb,getSettings}from"../../../../lib/db";
const bad=/(noreply|no-reply|privacy|abuse|webmaster|careers|jobs|recruit|investor|media|press|support|help|legal|security|billing|customer.?service)/i;
export async function POST(){try{const s=await getSettings();if(!s.dryRun)return Response.json({ok:false,error:"Promotion review is locked to Dry Run."},{status:409});const db=getDb();const rows=(await db.prepare(`SELECT id,company,division,service,website,contact_name,contact_email,source_name,source_url,evidence,ai_score,decision,decision_reason FROM discovery_candidates WHERE decision='Qualified' AND contact_name IS NOT NULL AND trim(contact_name)<>'' AND contact_email IS NOT NULL AND trim(contact_email)<>'' AND lead_id IS NULL ORDER BY ai_score DESC LIMIT 100`).all<any>()).results;let ready=0,skipped=0;for(const x of rows){const email=String(x.contact_email).toLowerCase();if(bad.test(email)){skipped++;continue}const suppressed=await db.prepare(`SELECT id FROM suppressions WHERE lower(email)=lower(?) LIMIT 1`).bind(email).first<any>();if(suppressed){skipped++;continue}const existing=await db.prepare(`SELECT id FROM leads WHERE lower(contact_email)=lower(?) LIMIT 1`).bind(email).first<{id:number}>();let leadId=existing?.id;if(!leadId){const r=await db.prepare(`INSERT INTO leads(company,division,service,website,contact_name,contact_email,source_name,source_url,buyer_evidence,qualification_reason,ai_score,priority,email_verified,qualification_status,pipeline_stage,outreach_status) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,1,'Qualified','Qualified','Ready for Review')`).bind(x.company,x.division,x.service??null,x.website??null,x.contact_name,email,x.source_name??null,x.source_url??null,x.evidence??null,x.decision_reason??null,x.ai_score,x.ai_score>=85?'High':'Medium').run();leadId=Number(r.meta?.last_row_id??0);ready++;}await db.prepare(`UPDATE discovery_candidates SET lead_id=? WHERE id=?`).bind(leadId,x.id).run();}return Response.json({ok:true,dryRun:true,emailSending:false,ready,skipped,message:`${ready} verified qualified contacts promoted to Ready for Review. No email sent.`});}catch(e){return Response.json({ok:false,error:e instanceof Error?e.message:"Promotion failed"},{status:500});}}
