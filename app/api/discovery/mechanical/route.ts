import{getDb,getSettings}from"../../../../lib/db";import{scoreCandidate}from"../../../../lib/discovery";import{searchMechanicalLeads}from"../../../../lib/mechanical-discovery";
export async function GET(){try{const s=await getSettings();if(!s.dryRun)return Response.json({ok:false,error:"Mechanical discovery is locked to Dry Run."},{status:409});const db=getDb();const sources=(await db.prepare(`SELECT name,url,source_type FROM discovery_sources WHERE enabled=1 ORDER BY source_type,name LIMIT 30`).all<any>()).results;if(!sources.length)return Response.json({ok:true,configured:false,dryRun:true,emailSending:false,error:"Add at least one active company/careers or RFP/RFQ source first."});const found=await searchMechanicalLeads(sources),runKey=`mechanical-v1-${new Date().toISOString().replace(/[:.]/g,"-")}`;const rr=await db.prepare(`INSERT INTO discovery_runs(run_key,status,dry_run,summary) VALUES(?,'Running',1,?)`).bind(runKey,`Mechanical Lead Discovery v1: ${found.length} candidates`).run();const runId=Number(rr.meta?.last_row_id??0);let qualified=0,review=0,rejected=0;const results=[];for(const input of found){const x=scoreCandidate(input);if(x.decision==="Qualified")qualified++;else if(x.decision==="Review")review++;else rejected++;await db.prepare(`INSERT INTO discovery_candidates(run_id,company,division,service,website,source_name,source_url,evidence,ai_score,decision,decision_reason) VALUES(?,?,?,?,?,?,?,?,?,?,?)`).bind(runId,x.company,"Engineering",x.service??null,x.website??null,x.sourceName??null,x.sourceUrl??null,x.evidence??null,x.score,x.decision,x.reason).run();results.push({company:x.company,needDetected:x.service,evidence:x.evidence,source:x.sourceName,sourceUrl:x.sourceUrl,score:x.score,decision:x.decision,reason:x.reason});}await db.prepare(`UPDATE discovery_runs SET status='Completed',engineering_found=?,software_found=0,qualified=?,rejected=?,summary=?,completed_at=CURRENT_TIMESTAMP WHERE id=?`).bind(results.length,qualified,rejected,`Mechanical v1: ${qualified} qualified, ${review} review, ${rejected} rejected. No email sent.`,runId).run();return Response.json({ok:true,source:"Mechanical Lead Discovery v1",dryRun:true,emailSending:false,runId,summary:{found:results.length,qualified,review,rejected},results});}catch(e){console.error("mechanical-discovery",e);return Response.json({ok:false,error:e instanceof Error?e.message:"Mechanical discovery failed"},{status:500});}}
