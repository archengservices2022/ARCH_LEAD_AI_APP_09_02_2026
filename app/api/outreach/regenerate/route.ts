import{getDb,getSettings}from"../../../../lib/db";
function firstName(n:string){return n.trim().split(/\s+/)[0]||"there"}
function draft(x:any){const engineering=x.division==="Engineering";const need=String(x.service||(engineering?"mechanical engineering and design support":"software and workflow automation")).trim();const subject=engineering?`${x.company} - engineering capacity support`:`${x.company} - software automation support`;const opening=engineering?`I’m reaching out because we identified current activity at ${x.company} related to ${need.toLowerCase()}. Arch Engineering Services helps engineering teams add experienced design capacity when schedules are tight, projects peak, or internal resources are committed elsewhere.`:`I’m reaching out because we identified current activity at ${x.company} related to ${need.toLowerCase()}. Arch Engineering Services helps teams replace manual, disconnected processes with practical software and automation built around the way their business actually operates.`;const capability=engineering?`We can support mechanical design and detailing, Inventor/SolidWorks/AutoCAD, fabrication and manufacturing drawings, BOMs, CAD/iLogic automation, reverse engineering, and overflow project work. We can step in for a defined package of work or operate as an extension of your engineering team.`:`We build custom business applications, workflow automation, integrations/APIs, dashboards, customer and internal portals, CRM/operations tools, and web/mobile applications. The focus is measurable: reduce repetitive work, improve visibility, and connect processes that currently require manual handoffs.`;const close=engineering?`If ${x.company} has a current design backlog, upcoming project, or a need for additional engineering bandwidth, I’d welcome a short conversation. I can also send a concise capability overview and relevant examples for your review.`:`If ${x.company} has a workflow that is still heavily manual, an integration gap, or an application initiative that needs additional development capacity, I’d welcome a short conversation. I can also send a concise capability overview and relevant examples for your review.`;return{subject,body:`Hi ${firstName(x.contact_name)},\n\n${opening}\n\n${capability}\n\n${close}\n\nWould a brief 15-minute conversation next week be worthwhile?\n\nBest regards,\nSridhar Kota\nArch Engineering Services\narchengineeringservices.com\n\nIf you prefer not to receive further outreach from us, reply and we will remove you from future messages.`}}
export async function POST(){try{const s=await getSettings();if(!s.dryRun)return Response.json({ok:false,error:"Draft regeneration is locked to Dry Run."},{status:409});const db=getDb();const rows=(await db.prepare(`SELECT o.id,o.lead_id,l.company,l.division,l.service,l.contact_name,l.contact_email FROM outreach_messages o JOIN leads l ON l.id=o.lead_id WHERE o.status='Queued' ORDER BY o.created_at DESC LIMIT 100`).all<any>()).results;let updated=0;for(const x of rows){if(!x.contact_name||!x.contact_email)continue;const d=draft(x);await db.prepare(`UPDATE outreach_messages SET recipient=?,subject=?,body=? WHERE id=? AND status='Queued'`).bind(x.contact_email,d.subject,d.body,x.id).run();updated++;}return Response.json({ok:true,dryRun:true,emailSending:false,updated,message:`${updated} queued outreach drafts regenerated with the stronger template. No email sent.`});}catch(e){return Response.json({ok:false,error:e instanceof Error?e.message:"Draft regeneration failed"},{status:500});}}
