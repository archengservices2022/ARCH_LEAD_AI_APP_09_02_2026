"use client";
import{useState}from"react";
export default function SoftwareLibraryButton(){const[busy,setBusy]=useState(false),[msg,setMsg]=useState("");async function load(){setBusy(true);setMsg("");try{const r=await fetch("/api/discovery/software-source-library",{method:"POST",headers:{Accept:"application/json"},cache:"no-store"});const text=await r.text();let j:any={};try{j=text?JSON.parse(text):{}}catch{throw new Error(`Software library returned ${r.status} without valid JSON`)}if(!r.ok)throw new Error(j.error||"Could not load Software source library");setMsg(`${j.added} new Software sources added.`);setTimeout(()=>location.reload(),700)}catch(e){setMsg(e instanceof Error?e.message:"Could not load Software library")}finally{setBusy(false)}}return <div className="libraryAction"><button onClick={load} disabled={busy}>{busy?"Loading Software Sources…":"Load Software Source Library"}</button>{msg&&<span>{msg}</span>}</div>}
