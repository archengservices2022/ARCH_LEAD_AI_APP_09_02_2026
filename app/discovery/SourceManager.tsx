"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export default function SourceManager(){const r=useRouter();const[name,setName]=useState("");const[url,setUrl]=useState("");const[type,setType]=useState("Company Website/Careers");const[msg,setMsg]=useState("");const[busy,setBusy]=useState(false);async function add(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");const res=await fetch("/api/discovery/sources",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,url,sourceType:type})});const data=await res.json();setBusy(false);if(!res.ok){setMsg(data.error||"Could not save source");return}setName("");setUrl("");setMsg("Source added.");r.refresh()}return <form className="sourceForm" onSubmit={add}><div><label>Source name</label><input value={name} onChange={e=>setName(e.target.value)} placeholder="Example Manufacturing Careers" required/></div><div><label>Source type</label><select value={type} onChange={e=>setType(e.target.value)}><option>Company Website/Careers</option><option>RFP/RFQ Portal</option></select></div><div className="sourceUrl"><label>Public URL</label><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://company.com/careers" required/></div><button disabled={busy}>{busy?"Saving...":"Add Source"}</button>{msg&&<span className="formMsg">{msg}</span>}</form>}
