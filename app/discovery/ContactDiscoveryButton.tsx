"use client";
import{useState}from"react";
export default function ContactDiscoveryButton(){const[busy,setBusy]=useState(false),[msg,setMsg]=useState("");async function run(){setBusy(true);setMsg("");try{const r=await fetch("/api/discovery/enrich-contacts",{method:"POST",headers:{Accept:"application/json"},cache:"no-store"});const j=await r.json();if(!r.ok)throw new Error(j.error||"Contact Discovery failed");setMsg(j.message||`Checked ${j.checked??0}; verified ${j.enriched??0}. No email sent.`);setTimeout(()=>location.reload(),2500)}catch(e){setMsg(e instanceof Error?e.message:"Contact Discovery failed")}finally{setBusy(false)}}return <div><button onClick={run} disabled={busy}>{busy?"Finding & Verifying Contacts…":"Run Contact Discovery"}</button>{msg&&<div style={{marginTop:12,padding:"12px 14px",border:"1px solid #cbd5e1",borderRadius:8,background:"#f8fafc",fontWeight:700,lineHeight:1.5}}>{msg}</div>}</div>}
