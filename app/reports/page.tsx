import Shell from "../components/Shell";import {getDb,getSettings} from "../../lib/db";export const dynamic="force-dynamic";export default async function Reports(){const db=getDb();const s=await getSettings();const rows=await db.prepare(`SELECT report_date,leads_found,qualified,engineering_sent,software_sent,total_sent,followups_sent,replies,interested,bounced,failed,skipped FROM daily_reports ORDER BY report_date DESC LIMIT 90`).all<any>();return <Shell active="/reports" dryRun={s.dryRun}><header><div><h1>Daily Reports</h1><p>Historical discovery, outreach and response performance</p></div></header><article className="panel wide"><div className="panelHead"><div><h2>Daily Performance</h2><p>Cloudflare D1 reporting history</p></div></div>{rows.results.length?<div className="tableWrap"><table><thead><tr><th>Date</th><th>Leads</th><th>Qualified</th><th>Eng Sent</th><th>Software Sent</th><th>Total</th><th>Replies</th><th>Interested</th><th>Failed</th></tr></thead><tbody>{rows.results.map((x:any)=><tr key={x.report_date}><td>{x.report_date}</td><td>{x.leads_found}</td><td>{x.qualified}</td><td>{x.engineering_sent}</td><td>{x.software_sent}</td><td>{x.total_sent}</td><td>{x.replies}</td><td>{x.interested}</td><td>{x.failed}</td></tr>)}</tbody></table></div>:<div className="empty">No daily reports yet.</div>}</article></Shell>}
